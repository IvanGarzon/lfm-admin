# ⚠️ NOTE: Replace these example values with your actual environment variables.

# Shared database settings
PGPORT=[PORT]
PGHOST=[HOST]
PGDATABASE=[DATABASE_NAME]
PGUSER=[DATABASE_USER]
PGPASSWORD=[DATABASE_PASSWORD]
PGTIMEOUT=[CONNECTION_TIMEOUT_SECONDS]
PGLIMIT=[CONNECTION_LIMIT]

# PostgreSQL connection URL (used by Prisma / NextAuth)
DATABASE_URL="postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}/${PGDATABASE}?sslmode=require&connect_timeout=${PGTIMEOUT}&connection_limit=${PGLIMIT}"

# Google OAuth credentials
GOOGLE_CLIENT_ID=[GOOGLE_OAUTH_CLIENT_ID]
GOOGLE_CLIENT_SECRET=[GOOGLE_OAUTH_CLIENT_SECRET]
AUTH_SECRET=[GENERAL_AUTH_SECRET]

# NextAuth settings
NEXTAUTH_SECRET=[NEXTAUTH_SECRET]
NEXTAUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true

# App - Don't add "/" in the end of the url (same in production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google Optimize API key (for A/B testing or feature flags)
OPTIMIZE_API_KEY=[GOOGLE_OPTIMIZE_API_KEY]

# Feature flag to enable database adapter (e.g., Prisma Adapter for NextAuth)
USE_ADAPTER=true

# AWS S3 Configuration (for quote attachments)
AWS_REGION=ap-southeast-2
AWS_ACCESS_KEY_ID=[AWS_ACCESS_KEY]
AWS_SECRET_ACCESS_KEY=[AWS_SECRET_KEY]
AWS_S3_BUCKET_NAME=lasflores-admin-uploads

RESEND_API_KEY=[RESEND_API_KEY]

# LocalStack Configuration (for local development only)
# Uncomment the line below when using LocalStack locally
# AWS_ENDPOINT_URL=http://localhost:4566


